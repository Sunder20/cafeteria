<!DOCTYPE html>
<html lang="en">
  <head>
    <script type="text/javascript"></script>
    <title>admin-Twenty & Six Espresso</title>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1.0, user-scalable=0, minimum-scale=1.0, maximum-scale=1.0">
    <link rel="stylesheet" type="text/css" href="home.css">
    <link rel="stylesheet" type="text/css" href="menu.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href='https://fonts.googleapis.com/css?family=Armata' rel='stylesheet'>
    <link href='https://fonts.googleapis.com/css?family=Atma' rel='stylesheet'>
    <link href='https://fonts.googleapis.com/css?family=Nixie One' rel='stylesheet'>
  </head>
  <body class="menus_customer-cartcontent">
    <main class="main">
      <h1 type="button" class="collapsible users_clerk-heading">
        Clerks
      </h1>
      <div class="menus">
        <% User.clerk.each do |clerk| %>
          <div class="flexing">
            <p class="menu_list">
              <%= clerk.name %>
            </p>
            <p class="menu_list">
              <%= clerk.email %>
            </p>
            <%= link_to "Remove Clerk" ,
              user_path(id: clerk.id) , method: :put ,
              :class => "menu_list users_clerk-removeclerk order_clerk-title",
              :onclick => "return confirm('Confirm remove clerk?');"
            %>
          </div>
        <% end %>
      </div>
      <h1 type="button" class="collapsible users_clerk-heading">
        Customers
      </h1>
      <div class="menus">
        <% User.customer.each do |customer| %>
          <div class="flexing">
            <p class="menu_list">
              <%= customer.name %>
            </p>
            <p class="menu_list">
              <%= customer.email %>
            </p>
            <%= link_to "Make Clerk" ,
              user_path(id: customer.id) ,
              method: :put ,
              :class => "menu_list users_clerk-removeclerk order_clerk-title",
              :onclick => "return confirm('Confirm make clerk');"
            %>
          </div>
        <% end %>
      </div>
    </main>
    <div class="zag slide">
    </div>
  </body>
</html>
