<!DOCTYPE html>
<html lang="en">
  <head>
    <script type="text/javascript"></script>
    <title>admin-Twenty & Six Espresso</title>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1.0, user-scalable=0, minimum-scale=1.0, maximum-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href='https://fonts.googleapis.com/css?family=Armata' rel='stylesheet'>
    <link href='https://fonts.googleapis.com/css?family=Atma' rel='stylesheet'>
    <link rel="stylesheet" type="text/css" href="home.css">
    <link rel="stylesheet" type="text/css" href="menu.css">
  </head>
  <body class="order_clerk-body">
    <main class="main">
      <div class="flexing">
        <div class="order_clerk-margin">
          <h3 class="Menupage-title order_customer-title" >Pending Orders</h3>
          <div class="menus">
            <div class="order-item">
              <ul>
                <% Order.confirmed.all.each do |order|%>
                  <li>
                    <label class='MyMenuItem-Container'>
                      <a   href="<%= order_path(id: "#{order.id}")  %>" , class="menu_list menus_customer-list">
                        <p><%= order.id %>&nbsp; &nbsp; &nbsp; &nbsp; <%= User.find(order.user_id).name %></p>
                      </a>
                      <%= link_to "Ready", "/orders/#{order.id}", method: :put , :class => "AddMenu-button orders_clerk-checkbox"  %>
                    </label>
                  </li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
        <div style="margin-right: 10px;">
          <h3 class="Menupage-title" style="text-align: center;">To be delivered</h3>
          <div class="menus">
            <div class="order-item" >
              <ul>
                <% Order.ready.all.each do |order|%>
                  <li>
                    <label class='MyMenuItem-Container'>
                      <a   href="<%= order_path(id: "#{order.id}")  %>" , class="menu_list menus_customer-list" >
                        <p><%= order.id %>&nbsp; &nbsp; &nbsp; &nbsp; <%= User.find(order.user_id).name %></p>
                      </a>
                      <%= link_to "Delivered", "/orders/#{order.id}", method: :put , :class => "AddMenu-button orders_clerk-button" %>
                    </label>
                  </li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      </div>
      <div class="zag slide"></div>
    </main>
  </body>
</html>
